# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Build and deploy container app to Azure Web App - lightspeed

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: 'ubuntu-latest'

    steps:
    - uses: actions/checkout@v2

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2

    - name: Log in to registry
      uses: docker/login-action@v2
      with:
        registry: https://lightspeed.azurecr.io/
        username: ${{ secrets.AzureAppService_ContainerUsername_2f9db319cba646a68b988e5efc287093 }}
        password: ${{ secrets.AzureAppService_ContainerPassword_a2b44a03e216494ebfa9f9a5964ee754 }}

    - name: Build and push container image to registry
      uses: docker/build-push-action@v3
      with:
        push: true
        tags: lightspeed.azurecr.io/${{ secrets.AzureAppService_ContainerUsername_2f9db319cba646a68b988e5efc287093 }}/lightspeed:${{ github.sha }}
        file: ./Dockerfile

  deploy:
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: 'production'
      url: ${{ steps.deploy-to-webapp.outputs.webapp-url }}

    steps:
    - name: Deploy to Azure Web App
      id: deploy-to-webapp
      uses: azure/webapps-deploy@v2
      with:
        app-name: 'lightspeed'
        slot-name: 'production'
        publish-profile: ${{ secrets.AzureAppService_PublishProfile_cd3a8bb5d54a4017aebd5065152acbe2 }}
        images: 'lightspeed.azurecr.io/${{ secrets.AzureAppService_ContainerUsername_2f9db319cba646a68b988e5efc287093 }}/lightspeed:${{ github.sha }}'